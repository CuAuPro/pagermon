{
    "name": "Discord",
    "description": "Send messages to a Discord webhook",
    "disable": false,
    "trigger": "message",
    "scope": "after",
    "config": [ 
        {
            "name": "useHttps",
            "label": "Use HTTPS",
            "description": "Force the hostname to use https:// in Discord embeds",
            "type": "checkbox",
            "default": true
        }
    ],
    "aliasConfig": [ 
        {
            "name": "enable",
            "label": "Enable",
            "description": "Send messages matching this alias to Discord webhook",
            "type": "checkbox"
        },
        {
            "name": "webhook",
            "label": "Webhook",
            "description": "Full URL of Discord webhook",
            "type": "text",
            "required": true
        }
    ]
}